import 'package:flutter/material.dart';

class Mapash extends StatelessWidget {
  const Mapash({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Mapa Historial'),
    );
  }
}
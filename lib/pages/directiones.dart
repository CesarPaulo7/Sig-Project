import 'package:flutter/material.dart';

class Directiones extends StatelessWidget {
  const Directiones({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('DIRECCIONES'),
    );
  }
}